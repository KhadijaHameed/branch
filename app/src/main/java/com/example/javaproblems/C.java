package com.example.javaproblems;

import android.view.View;
import android.widget.Toast;

public class C {

   /*  btnCheck.setOnClickListener(new View.OnClickListener() {
        @Override
        public void onClick(View v) {
            String input = etORiginal.getText().toString();

            int length = input.length();
            for ( int i =  length- 1; i >= 0; i-- )
                reverse = reverse + input.charAt(i);
            if (input.equals(reverse)){
                Toast.makeText(MainActivity.this, ":)string/number is a palindrome.", Toast.LENGTH_SHORT).show();
            }else {
                Toast.makeText(MainActivity.this, "@.@string/number IS NOT  a palindrome.", Toast.LENGTH_SHORT).show();
            }

            Toast.makeText(MainActivity.this, "input "+input, Toast.LENGTH_SHORT).show();
        }
    });
*/
}
